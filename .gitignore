node_modules/
.env
dist/
*.log
daemon/
.playwright-mcp/

.env.test
.env.*.local

# stray local artifacts / runtime data
server/data/
/info-sheet-submitted.png
/leasing-executive-dashboard.html

# operational provisioning scripts (real credentials/PII — keep out of the repo)
server/scripts/create-*.js

# Database dumps — contain the passwordPlain column for every user.
# This repository is public; these must never be committed.
backups/
