// Re-export the shared source of truth so the form/table and the server
// validation stay in lockstep.
export { INQUIRER_TYPES, INQUIRER_LABEL, INQUIRY_TYPES } from "../../../shared/inquiryTypes.js";
